{
  "name": "oak",
  "version": "0.0.0",
  "private": true,
  "description": "Local-file-first knowledge graph",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "oak": "node packages/cli/dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
